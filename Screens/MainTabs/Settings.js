import React from 'react';

import { SafeAreaView, Text, StatusBar } from 'react-native';

const Settings = () => {
  return (
    <SafeAreaView>
      <StatusBar />
    </SafeAreaView>
  );
};

export default Settings;
